function makeNegative(num) {
  //check if number is negative
  if(num < 0) {
    //return number un altered if not
     return num 
     } else {
        //return num mulitiplied by -1 if not
        return num * -1
  }
}